import numpy as np

arr = ['A','B','C']


ind = 0
sco = 0
while time <= 5
while ind <= len(arr)-1: #for continuous index access once answer is correct
    inp = input("Enter letter in Index " + str(ind) + ": ")
    if inp == arr[ind]:
        ind = ind + 1
        sco = sco + 1
    else:
        break

